import java.util.*;
public class code3 {
    static int a;
    public static void main(String[] args) {
        // System.out.println("Hello, World!");
        // put();
        // get(),containsKey(),containsValue(),remove(),getKey(),getValue(),
        // clear(),size(), keySet(),values(),putIfAbsent(
        int s;
        System.out.println(a);
        
        // Map<String, Integer> map = new HashMap<>();
        // map.put("A", 1);
        // map.put("B", 2);
        // map.put("C", 3);

        // PriorityQueue<int[]>pq = new PriorityQueue<>(Comparator.comparingInt(a->a[1]));
        // pq.offer(new int[]{123,23});
        // pq.offer(new int[]{20,32});
        // pq.offer(new int[]{90,12});

        // // System.out.println(pq);
        // while (!pq.isEmpty()) {
        //     int[] arr= pq.poll();
        //     System.out.println(arr[0] + " " + arr[1]);
        // } 
    }
}
